version: '3.6'

services:
  superset:
    image: "<%= @utils['images']['superset'] %>"
    restart: always
    ports:
      - "8088:8088"
    volumes:
      - ../data/superset:/var/lib/superset

volumes:
  superset_db:
